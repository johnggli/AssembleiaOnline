<%= simple_form_for @assembly, :url => url, :method => method  do |p| %>
  <%= p.input :title %>
  <%= p.input :start_time %>
  <%= p.input :finish_time %>
  <%#= p.input :url_ata %>
  <%= p.rich_text_area :description %>
  
  
  <%= p.submit controller.action_name == 'edit' ? 'Atualizar Assembléia' : 'Criar Assembléia' %>
<% end %>