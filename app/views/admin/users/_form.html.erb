<%= simple_form_for @user, :url => url, :method => method  do |p| %>
    <%= p.input :user_name %>
    <%= p.input :pre_registration_id %>
    <%= p.input :email %>
    <%= p.input :password %>
    <%= p.input :password_confirmation %>

    <%= p.submit 'Registrar' %>
  <% end %>